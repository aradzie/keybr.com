{
  "private": true,
  "name": "@keybr/database",
  "version": "0.0.0",
  "type": "module",
  "main": "lib/index.ts",
  "types": ".types/index.d.ts",
  "dependencies": {
    "@keybr/config": "*",
    "@keybr/names": "*",
    "@keybr/oauth": "*",
    "@keybr/pages-shared": "*",
    "@keybr/publicid": "*",
    "@keybr/rand": "*"
  },
  "devDependencies": {},
  "scripts": {
    "clean": "rm -fr .types",
    "compile": "tsc",
    "test": "node --import @keybr/tsl --import=@keybr/test-env-server --test 'lib/**/*.test.{ts,tsx}'"
  }
}
